# environment_requirements.txt - curated top-level requirements (research venv)
# Generated 2026-09-04 for XAUUSD AI Quant research environment.
# Locked reproduction: use configs/requirements-lock.txt (pip freeze, exact).
# Install into a fresh venv:  pip install -r configs/requirements-lock.txt
# (torch is the +cu126 wheel from https://download.pytorch.org/whl/cu126;
#  system index mirror used locally: pypi.tuna.tsinghua.edu.cn)

# ---- Core scientific ----
numpy==2.5.2
pandas==3.0.5
scipy==1.18.1
numba==0.67.0
pyarrow==25.0.1
polars==1.44.1
fastparquet==2026.5.0
openpyxl==3.1.5
duckdb==1.5.5
bottleneck==1.6.0

# ---- Statistics / time series ----
statsmodels==0.15.0
arch==8.0.0

# ---- ML ----
scikit-learn==1.9.0
xgboost==3.4.1
lightgbm==4.7.0
torch==2.14.0+cu126  # PyTorch official CUDA 12.6 wheel (GPU runtime bundled; driver 591.55)

# ---- Backtest / research libs ----
vectorbt==1.1.0
backtesting==0.6.6
pandera==0.33.1
hydra-core==1.3.6

# ---- Validation / quality ----
pytest==9.1.1
pytest-cov==7.1.0
ruff==0.16.6
black==26.5.1

# ---- Config / io / util ----
PyYAML==6.0.3
tomli==2.4.1
python-dotenv==1.2.3
psutil==7.2.2
tqdm==4.70.0
joblib==1.6.0
matplotlib==3.11.1

# ---- Market data (optional read-only) ----
MetaTrader5==5.0.6162  # requires installed MT5 terminal; read-only usage only
