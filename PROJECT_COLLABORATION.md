# PROJECT_COLLABORATION.md

> 本文件定义本项目（AIQuant / Hermes 交易系统）的**协作契约与边界**。
> 所有参与方（OpenClaw、Hermes、ChatGPT、GitHub）在行动前必须遵守本文件。
> 版本：v1（2026-09-19 初始建立）

---

## 1. 角色分工（Roles）

| 角色 | 定位 | 职责 | 禁止 |
|------|------|------|------|
| **OpenClaw** | 本地执行与运行控制 | 在本机执行命令、调度（cron/计划任务）、读取与整理文件、运行/监控引擎、生成报告 | 不做交易决策、不越权改策略、不外发敏感数据 |
| **Hermes** | 交易智能 | 交易决策、风控、账本、机会评估、仓位管理（OBSERVE→THINK→DECIDE→PLAN→TRIGGER→EXECUTE→MANAGE→REVIEW→MEMORY） | 不由外部直接改其逻辑；不得绕过安全门 |
| **ChatGPT** | 独立研究 / 审计 | 独立的研究、复核、审计与质疑；对方案与结论提出独立判断 | 不直接操作本机运行环境、不直接改代码/配置 |
| **GitHub** | 共享项目事实层 | 代码 / 配置 / 研究报告 / 审计报告 / 任务记录的**共享事实来源**（single source of truth） | 不得承载密钥、凭据、个人敏感信息或运行中的私密数据 |

原则：**执行（OpenClaw）/ 智能（Hermes）/ 审计（ChatGPT）三方分离**，通过 GitHub 事实层对齐，避免单点既当运动员又当裁判。

---

## 2. 版本隔离（Version Isolation）

- **V1 / V2 / V3 相互隔离**，互不干扰：
  - 各自独立的目录（`trader_v1` / `trader_v2` / `trader_v3`）、独立配置、独立运行态、独立账户/魔术号（magic）。
  - **不得跨版本修改**：改动任一版本不得触碰其他版本的代码、配置、运行态或账本。
  - V1 默认**冻结只读**，仅观察。
- 任何跨版本改动必须单独记录并说明影响面。

---

## 3. V2 当前阶段（Current Phase）

- **V2 当前为 Shadow / Paper 阶段**（纸面 / 影子运行，非实盘）。
- 允许的最小验证形态：**Demo/Paper + Forward 观察**；**实盘（live）硬锁关闭**。
- 在取得充分 forward 证据并通过验收前，不进入真实资金交易。

---

## 4. 行情源纪律（Market Data Discipline）—— V2 硬规则

1. **MT5 是 V2 唯一的交易决策行情源。**
   - 交易决策（技术面 / 定价 / 执行相关）只能基于 MT5 行情。
   - 其它来源（本地 tick 重建、第三方行情等）不得进入 V2 的**决策与执行价格空间**；参考市场（如 GC=F）仅作 reference，不得替代主源。

2. **MT5 异常必须安全 WAIT，不得 fallback 后继续交易。**
   - 当 MT5 行情缺失 / 断流 / 超时 / 校验失败时：**安全降级为 WAIT**（不决策 / 不开仓）。
   - **严禁**：在 MT5 不可用时回退到次级/缓存行情，然后**照常决策或交易**。
   - 降级必须**可见**（记录 gap/状态），不得静默用旧数据顶替。

---

## 5. 安全规则（Security Rules）—— 推送到 GitHub 时强制

**严禁上传**：
- 账号 / 密码（含 MT5 登录凭据：login / password / server）
- Token / API key / 私钥 / 证书（`*.key` `*.pem` `*.pfx` `id_rsa` 等）
- `.env` / `.env.*` 等凭据文件
- 个人敏感信息
- **运行中的私密数据**（实时/运行态账本、持仓与账户明细、tick/行情原始数据、私有缓存与证据原文等）

**要求**：
- 只上传：代码、配置模板、研究报告、审计报告、任务记录等**非私密**内容。
- 上传前必须做密钥扫描（工作树 + 全历史）。
- 发现凭据必须**先脱敏/移除再上传**，并记录处置。
- 不删除本地历史证据（脱敏在副本/新历史中进行）。

---

## 6. 变更与审计流程（Change & Audit）

1. 任何改动前：先只读诊断，确认性质（BUG vs 设计 vs 优化）。
2. 写操作 / 配置 / 调度变更：先说明，取得同意。
3. 交易逻辑、风控、策略、冻结基线：**未经明确授权不得改动**。
4. 审计留痕：报告、证据、决策记录写入项目事实层，保持可回溯。
5. 结论与证据分离：**不假设，用事实**（能核则核）。

---

_本文件为协作契约的初始版本，随项目演进而更新；更新需在此记录版本与日期。_
