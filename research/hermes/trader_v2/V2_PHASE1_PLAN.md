# Hermes V2 — 第一阶段建设计划（执行用）

> 目标：**先把完整系统真实跑起来**（任务书 §十五/§十六），不追收益、不调参、不回测假设。

## 阶段划分与交付
| # | 交付 | 关键点 | 状态 |
|---|---|---|---|
| 0 | 隔离骨架 + 架构/计划/配置 | 已建 `trader_v2/` + `V2_ARCHITECTURE.md` + `config/v2_config.json` | ✅ |
| 1 | Agent1 技术情报 | 行情采集(独立源, point-in-time) + 多周期结构/波动/VWAP/EMA + 结构化情报 | ⬜ |
| 2 | Agent2 宏观/地缘/资金流 | Yahoo/财政部/ECB/CFTC/ETF/新闻采集(带发布时刻) + 结构化情报 + 背离标记 | ⬜ |
| 3 | Hermes V2 决策 | 决策契约 + 20 步机会发现 + 非投票 + "为什么没交易"记录 | ⬜ |
| 4 | Engine V2 + Ledger + 模拟账户 | 独立 sha256 ledger(含 provenance) + 本地 paper 账户(成本模型) + 触发/持仓/对账 | ⬜ |
| 5 | 编排 + 复盘 + 监控 | OpenClaw cron(独立 session) + 每日复盘 + 异常自恢复 | ⬜ |
| 6 | 端到端冒烟 + 完成标准核验 + 报告 | A–G 逐条核验；V1 未受干扰核验 | ⬜ |

## 需用户确认的决策（阻塞或影响形态）
1. **执行通道**（推荐默认已设 `paper_local`）：
   - (A) 本地独立 paper 账户（零依赖，立即可跑，成本模型拟真）← 推荐 Phase-1 用
   - (B) OANDA v20 demo（更真实成交，但需你花 ~10 分钟注册 OANDA + 生成 personal token）
   → 不改的话我按 (A) 起跑，接口留好，(B) 随时插。
2. **目录/命名**：采用 `research/hermes/trader_v2/`（V1 在 `trader_v1/`）——若你想用别的名字/位置，告诉我。
3. **节奏**：A1 每 15min / A2 每 60min / Hermes 每 15min（每轮约 2–3 次 LLM 调用，有 token 成本）。是否接受该频率？
4. **行情源**：V2 用 Yahoo（独立于 V1 的 MT5 采集）。是否 OK？（也可改成只读 V1 的 live tick——但那会与 V1 数据耦合，不推荐。）

## 落地约定（不改 V1）
- V2 所有写入仅限 `trader_v2/` 内；对 `trader_v1/`、`data/live_fxtm`、V1 的 cron 一律**只读或不碰**。
- 每步交付后跑测试 + 冒烟，报告"已验证/仍假设/数据缺口"。
- git：V2 变更单独提交（不与 V1 run_state 混提交）。

## 完成标准（对应任务书 §十六）
A Agent1 稳定情报 · B Agent2 稳定情报 · C Hermes 取双情报主动找机会 · D 独立模拟账户执行 · E 完整 ledger · F V1 未受干扰 · G 可审计(4 问)。
> 全部达成 → 先观察运行，不立即调参；样本足够再进 Phase-2（机会质量分析与进化）。
